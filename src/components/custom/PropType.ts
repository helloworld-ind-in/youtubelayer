export interface ProjectType {
  _id: string;
  title: string;
  description: string;
}

export interface ProjectEditorType {
  _id: string;
  email: string;
}

export interface VideoType{
  _id: string,
  name: string, 
  userId: string
}